---
description: "Shorthand for /newsletter-ai — weekly AI intelligence brief"
argument-hint: "[-o md|yaml] [-r path/to/folder-or-report.html]"
---

`/nltr-ai` is the shorthand for the `newsletter-ai` skill
(`skills/newsletters/newsletter-ai/SKILL.md`) — treat it identically to
`/newsletter-ai`. Invoke the `newsletter-ai` skill now, passing it
exactly the arguments given here, unparsed: $ARGUMENTS
