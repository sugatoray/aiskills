# MacBook scroll sequence example

```text
/visualize [[ MacBook exploding into components ]] | /scroll-sequence
```

Expected result: a pinned, reversible product sequence whose visual state is directly controlled by scroll progress.
