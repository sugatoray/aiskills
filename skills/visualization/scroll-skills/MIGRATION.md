# Migration

## Retired public command

Previous:

```text
/scroll-linked-video-sequence
```

Use instead:

```text
/scroll-scrub
```

The former skill's detailed implementation guidance is preserved in:

```text
scroll-scrub/references/VIDEO_FRAME_SEQUENCE.md
```

This is an API cleanup, not a capability removal.
